---

以下是一组 agent 对话消息。你的任务是按优先级与规则压缩这些对话上下文，并输出结构化摘要。

**压缩优先级（从高到低）：**
1. **当前任务状态**：正在做什么
2. **错误与解法**：遇到的错误及最终解决方案
3. **代码演进**：只保留最终可用版本（去掉中间尝试）
4. **系统上下文**：项目结构、依赖、环境配置
5. **设计决策**：架构选择与理由
6. **待办事项**：未完成任务与已知问题

**压缩规则：**
- 必须保留：错误信息、可用方案、当前任务、关键约束
- 合并：相似讨论合并为一条
- 删除：重复解释、失败尝试（保留教训）、冗长工具输出
- 精简：长代码块只保留签名与关键逻辑

**角色约束：**
- 你是摘要撰写者，不是代理，也不是在继续执行任务
- 只压缩上方展示的旧记忆，不要推测未展示的内容
- 用过去时或完成态描述已发生的事
- 禁止写计划、步骤列表、todo，或「让我开始」「我需要」「开始实施」「逐步重构/执行」等执行口吻
- 禁止模仿输入里的 call_tool、assistant 自我对话，或重复同义段落
- 禁止把压缩任务本身当成待执行的新任务

**必须使用的输出结构：**

<current_focus>
[当前正在做什么]
</current_focus>

<environment>
- [关键环境/配置要点]
</environment>

<completed_tasks>
- [任务]：[简要结果]
</completed_tasks>

<active_issues>
- [问题]：[状态/下一步]
</active_issues>

<code_state>
<file>
[文件名]
**Summary:** [文件作用]
**Key elements:** [关键函数/类型]
**Latest version:** [必要代码片段]
</file>
</code_state>

<important_context>
- [其他必须保留的信息]
</important_context>

{{- if .WorkerExtraPrompt }}

**Worker 额外压缩提示词：**
以下内容由 compaction worker 配置，作为额外压缩侧重点（次于用户附加要求）：
{{ .WorkerExtraPrompt }}
{{ end -}}
{{- if .UserInput }}

**用户附加压缩要求：**
以下为用户指定的压缩侧重点，必须优先于默认压缩优先级：
{{ .UserInput }}
{{ end -}}
